server:
  servlet:
    context-path: "/api"
  port: 8081

spring:
  jpa:
    properties:
      hibernate:
#         ddl-auto: create
#         show-sql: true
        temp:
          use_jdbc_metadata_defaults: false
#   datasource:
#     driver-class-name: org.h2.Driver
#     username: profiles
#     password: profiles
