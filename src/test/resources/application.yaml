server:
  servlet:
    context-path: "/api"
  port: 8081

spring:
  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
  #    show-sql: true
  #    hibernate:
  #      ddl-auto: create
  datasource:
    driver-class-name: org.h2.Driver
    username: profiles
    password: profiles